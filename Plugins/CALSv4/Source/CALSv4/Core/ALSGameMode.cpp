#include "ALSGameMode.h"

#include "Player/ALSCharacter.h"
#include "Player/ALSPlayerController.h"
#include "Utilities/ALSHelpers.h"

AALSGameMode::AALSGameMode() {
	/*const ConstructorHelpers::FObjectFinder<UBlueprintGeneratedClass> ALSPlayerController(TEXT("Blueprint'/CALSv4/BP_ALSController.BP_ALSController'"));
	if (ALSPlayerController.Succeeded())
		PlayerControllerClass = ALSPlayerController.Object->StaticClass();
	else
		UALSLogger::LogError("BP_ALSController not found.");

	const ConstructorHelpers::FObjectFinder<UBlueprintGeneratedClass>ALSCharacter(TEXT("Blueprint'/CALSv4/BP_ALSCharacter.BP_ALSCharacter'"));
	if (ALSCharacter.Succeeded())
		DefaultPawnClass = ALSCharacter.Object->StaticClass();
	else
		UALSLogger::LogError("BP_ALSCharacter not found.");*/
}